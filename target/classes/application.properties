spring.application.name=RedisJava
server.port=9000

# Load grocery JSON into Redis at startup (set false in tests)
app.grocery.load-on-startup=true

# Redis (override host/port/password via env or profile)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
